class Analytics {
  static Future<void> log_error(
      String name, Map<String, Object> parameters) async {
    // await FirebaseCrashlytics.instance.recordError(error, stackTrace,
    //     reason: 'a fatal error',
    //     // Pass in 'fatal' argument
    //     fatal: true);
  }
}
