import 'package:flutter/cupertino.dart';

const double defaultMargin = 10.0;
const EdgeInsets scaffoldPadding = EdgeInsets.all(defaultMargin);
const EdgeInsets scrollPadding =
    EdgeInsets.fromLTRB(defaultMargin, defaultMargin, defaultMargin, 0);

const double defaultPadding = defaultMargin / 1.75;
