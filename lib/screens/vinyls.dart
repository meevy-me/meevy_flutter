import 'package:flutter/material.dart';

class VinylScreen extends StatelessWidget {
  const VinylScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
